# 更新记录

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 的组织方式。正式版本发布后再采用语义化版本编号。

## [Unreleased]

### Added

- Jelly Studio 项目工作区、任务运行恢复、成果发现与多标签预览。
- 文件修改快照、Diff、安全恢复和可复用工作配方。
- Markdown 阅读目录、专注模式、隔离 HTML 预览与图片预览。
- Web 访问口令页面、错误边界、首次使用引导和明暗主题。
- GitHub Actions、依赖审计、容器健康检查以及发布与安全文档。
- Python wheel 与源码包内置已构建的 Jelly Studio，可直接从安装包启动 Web 界面。
- 可逆的项目归档与恢复；归档保留任务和文件，并阻止新的 Agent 执行。
- HTML 成果默认静态预览，并提供显式开启的无网络隔离交互模式。
- 新项目可一键创建专属工作目录，也可绑定现有目录；创建前显示目标位置，重名时不会覆盖已有文件。
- 项目记录改为同步写入后原子替换；自动创建的目录在支持的平台上使用仅当前用户可访问的权限。
- 任务启动失败或服务重启时，尚未写入会话历史的输入可从运行记录恢复到输入框；运行记录写盘改为同步写入与原子替换。
- 任务文件页可导出带对话、当前成果、版本来源和校验摘要的交付包，排除 Provider 凭据与常见密钥文件。
- 损坏的项目记录从列表中隔离并给出提示；会话写盘改为原子替换，删除任务时同步清理变更与运行记录。

### Changed

- 对话页重构为项目导航、对话执行区和成果区三栏工作台。
- 项目可保存默认模型与执行权限，任务固定使用所属项目目录。
- 上下文裁剪优先保留最近消息；会话 ID 使用更高熵随机值。
- 非回环 Web 服务必须启用访问鉴权。
- 任务状态轮询支持 ETag 条件请求，空闲时降低轮询频率。
- React 构建链升级至 Vite 8，Markdown 与 HTML 净化依赖升级至当前安全版本。
- 容器默认使用非 root 用户运行，工作区与配置目录显式持久化。
- 文件已成功写入但快照超限时保留真实执行结果，避免 Agent 因误判失败而重复写入。

### Security

- Bash 子进程过滤服务进程中的常见凭据环境变量。
- 原始文件下载与图片预览改用请求头传递访问口令。
- 文件预览采用有界读取，客户端消息设置长度上限。
- 增加 CSP、禁止嵌入、禁止 MIME 猜测、隐私与权限策略等浏览器安全响应头。
- WebSocket 断开时等待执行器安全退出，避免提前释放同一会话的运行锁。
- Web 鉴权使用常量时间比较，并拒绝不安全的多用户标识和重复口令。
- 交互 HTML 预览使用短时单次地址与浏览器沙箱；Qwen 启动脚本避免把访问口令放入命令行参数，Compose 默认仅绑定本机端口。
