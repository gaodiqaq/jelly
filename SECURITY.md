# 安全说明

## 支持范围

Jelly 当前按“单个可信操作者、本地或可信网络运行”的产品边界设计。项目中的 Agent 可以读写工作区、运行命令并访问网络；授予自动执行权限等同于允许所选模型在该工作区内使用这些能力。

`AGENT_WEB_USERS` 只为可信团队分开会话目录。Provider 配置、Skill、进程权限与宿主文件系统仍然共享，因此它不是不可信租户之间的安全隔离机制。

## 安全部署

- 本机使用时监听 `127.0.0.1`。监听任何非回环地址前必须设置强随机 `AGENT_WEB_TOKEN`。
- 不要把开发服务器直接暴露到公网。远程访问应经过 TLS、私有网络或具有访问控制的反向代理。
- 为 Jelly 使用独立、最小权限的模型凭据，并限制可访问的工作目录。
- 对未知项目先使用“只读”或“手动审批”；确认命令影响后再启用自动执行。
- `./data`、`~/.agent_shell`、会话和项目目录可能包含提示词、文件内容和凭据配置，应按敏感数据备份与保护。
- Bash 子进程会过滤常见凭据环境变量；这不能替代独立操作系统账户、容器或沙箱。

## 泄露凭据处置

一旦凭据进入终端输出、日志或 Git 历史，应立即在提供商侧吊销并重新签发。仅从当前文件删除并不能清除已有提交中的内容；还需要清理仓库历史及其镜像、缓存和派生仓库。

## 报告问题

请优先通过 GitHub 仓库的 **Security Advisories / Report a vulnerability** 私下报告漏洞。报告中提供受影响版本、复现条件、实际影响和最小复现步骤；请避免在公开 Issue 中附带有效凭据或用户数据。
